package(default_visibility = ["//visibility:public"])

filegroup(
    name = "config",
    srcs = glob(["config/**"], allow_empty = True),
)

filegroup(
    name = "templates",
    srcs = glob(["templates/**"], allow_empty = True),
)
