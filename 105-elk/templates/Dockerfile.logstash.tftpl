FROM docker.elastic.co/logstash/logstash:${elk_version}

# Prometheus metrics are exposed via the logstash-exporter sidecar (port 9198).
# Keep the base image unmodified to avoid JRuby plugin instability in LXC.
