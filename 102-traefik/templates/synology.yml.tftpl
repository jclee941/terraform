http:
  routers:
    nas:
      rule: "Host(`nas.${domain}`)"
      entryPoints:
        - websecure
      service: nas
      tls:
        certResolver: cf
      priority: 10
  services:
    nas:
      loadBalancer:
        servers:
          - url: "http://${hosts.synology.ip}:${hosts.synology.ports.dsm}"
