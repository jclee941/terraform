package(default_visibility = ["//visibility:public"])

filegroup(
    name = "config",
    srcs = glob(["config/**"], allow_empty = True),
)

filegroup(
    name = "runtime",
    srcs = [
        "docker-compose.yml",
        "glitchtip.env",
    ],
)
