name: Ollama Apply

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Skip deprecated workspace
        run: |
          echo "109-ollama workspace was removed (migrated to Synology 215); apply workflow disabled."
