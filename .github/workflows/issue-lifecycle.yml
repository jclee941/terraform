name: Issue Lifecycle

on:
  pull_request:
    types: [opened, edited, closed]
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  issue-lifecycle:
    uses: ./.github/workflows/_issue-lifecycle.yml
    secrets: inherit
